<?php

declare(strict_types=1);

return [
    /*
    |--------------------------------------------------------------------------
    | 部署级安装令牌（可选）
    |--------------------------------------------------------------------------
    |
    | Web 安装向导（/install）在 APP_KEY 为空、CSRF 豁免期间对外可访问。
    | 留空（默认）：向导直接放行，适合「上传源码解压即访问 /install」的简版部署；
    | 此时安装入口对可达网络开放，但安装锁存在后向导即 404，重装需先删除
    | storage/app/install.lock，风险可控。
    |
    | 如需额外收口，可在 .env 设置 INSTALL_TOKEN（强随机串），首次访问 /install
    | 后在安装页「安装令牌」输入框填写（随请求以请求头 X-Install-Token、URL 参数
    | token 或请求体 install_token 提交），不匹配一律 404。令牌不写入生成的 .env。
    |
    */

    'token' => (string) env('INSTALL_TOKEN', ''),
];
